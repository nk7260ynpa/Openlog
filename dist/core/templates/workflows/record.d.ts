/**
 * `/oplg:record` workflow: turn the most recent changes into a documentation
 * record under `openlog/` and update internal docs as needed.
 *
 * Outputs:
 *   - Skill: `openlog-record`
 *   - Command: `/oplg:record`
 */
import type { SkillTemplate, CommandTemplate } from '../types.js';
export declare function getRecordSkillTemplate(): SkillTemplate;
export declare function getOplgRecordCommandTemplate(): CommandTemplate;
//# sourceMappingURL=record.d.ts.map